# mesync bundle 配置层
# 作为 dsh.bundle 被引用，插件行的 name 用包名（dsh-mesync）解析，
# id 保持 mesync（短 id，供 patch 定位）。
- insert:
    - id: mesync
      name: 'dsh-mesync'
      config:
        autoExtract: true
        maxContextDecisions: 5
